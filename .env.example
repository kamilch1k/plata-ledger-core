# Postgres connection string for the server (the test suite does NOT need this —
# it boots its own ephemeral Postgres).
DATABASE_URL=postgres://postgres:postgres@localhost:5432/ledger?sslmode=disable

# HTTP port (default 8080).
PORT=8080
